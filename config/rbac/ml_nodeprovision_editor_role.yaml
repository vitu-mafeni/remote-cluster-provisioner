# This rule is not used by the project remote-cluster-provisioner itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the ml.dcn.ssu.ac.kr.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: remote-cluster-provisioner
    app.kubernetes.io/managed-by: kustomize
  name: ml-nodeprovision-editor-role
rules:
- apiGroups:
  - ml.dcn.ssu.ac.kr
  resources:
  - nodeprovisions
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ml.dcn.ssu.ac.kr
  resources:
  - nodeprovisions/status
  verbs:
  - get
