---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: remoteclusters.infra.dcn.ssu.ac.kr
spec:
  group: infra.dcn.ssu.ac.kr
  names:
    kind: RemoteCluster
    listKind: RemoteClusterList
    plural: remoteclusters
    singular: remotecluster
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RemoteCluster is the Schema for the remoteclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of RemoteCluster
            properties:
              auth:
                properties:
                  passwordSecretRef:
                    description: PasswordSecretRef holds the secret reference for
                      password-based SSH authentication.
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  sshPrivateKeySecretRef:
                    description: |-
                      SSHPrivateKeySecretRef holds the secret reference for private-key based SSH authentication.
                      The secret value should be the raw private key data.
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                type: object
              clusterName:
                description: foo is an example field of RemoteCluster. Edit remotecluster_types.go
                  to remove/update
                type: string
              gitConfig:
                properties:
                  enable:
                    type: string
                  gitServer:
                    type: string
                  gitUsername:
                    type: string
                  packageRevision:
                    type: string
                  upstreamPlatformRepo:
                    description: UpstreamPlatformRepo is the name of the git repository
                      in the management cluster that serves as the source of truth
                      for platform configuration.
                    type: string
                required:
                - gitServer
                - gitUsername
                - packageRevision
                - upstreamPlatformRepo
                type: object
              host:
                type: string
              nodeInfo:
                properties:
                  hardwareType:
                    type: string
                  nodeType:
                    type: string
                  softwareConfig:
                    properties:
                      cnlabRuntime:
                        description: |-
                          CnlabRuntime configures the prebuilt cnlab-runtime OCI artifact installation.
                          All fields are optional; defaults apply when omitted.
                        properties:
                          credentialsRef:
                            description: |-
                              CredentialsRef references a Secret with "username" and "token" keys
                              for authenticating to the OCI registry. Follows the same pattern as
                              VPNSSHCredentialsRef — set Name to enable secret lookup.
                            properties:
                              key:
                                type: string
                              name:
                                type: string
                              namespace:
                                type: string
                            type: object
                          orasVersion:
                            description: 'OrasVersion is the ORAS CLI version to install.
                              Default: "1.3.2"'
                            type: string
                          registry:
                            description: 'Registry is the OCI registry host. Default:
                              "ghcr.io"'
                            type: string
                          repository:
                            description: 'Repository is the image repository path.
                              Default: "vitu-mafeni/cnlab-runtime"'
                            type: string
                          version:
                            description: 'Version is the artifact version tag. Default:
                              "1.0.0-beta"'
                            type: string
                        type: object
                      imagePrepulls:
                        items:
                          description: ImagePrepull describes a single image to pre-pull
                            and which node types should pull it.
                          properties:
                            image:
                              description: Image is the fully-qualified container
                                image reference.
                              type: string
                            nodeTarget:
                              default: all
                              description: |-
                                NodeTarget controls which worker nodes pull this image.
                                "gpu"  — GPU workers only.
                                "all"  — every worker node (CPU and GPU).
                              enum:
                              - gpu
                              - all
                              type: string
                          required:
                          - image
                          type: object
                        type: array
                      imagePullSecretRef:
                        description: |-
                          ImagePullSecretRef optionally references a Secret containing registry
                          credentials used when pre-pulling private images listed in ImagePrepulls.
                          The Secret must have "username" and "password" keys.
                        properties:
                          key:
                            type: string
                          name:
                            type: string
                        required:
                        - name
                        type: object
                      kubernetesVersion:
                        description: |-
                          NvidiaDriverVersion           string   `json:"nvidiaDriverVersion,omitempty"`
                          NvidiaContainerToolkitVersion string   `json:"nvidiaContainerToolkitVersion,omitempty"`
                          K8sDevicePluginVersion        string   `json:"k8sDevicePluginVersion,omitempty"`
                        type: string
                      platformVariables:
                        description: |-
                          PlatformVariables holds cluster-specific key/value pairs injected into
                          every PackageVariant's spec.packageContext.data.  Each key overrides the
                          corresponding default in the package's committed package-context.yaml.
                        items:
                          description: |-
                            PlatformVariable is a single key/value pair injected into each PackageVariant's
                            spec.packageContext.data, overriding the package's committed defaults.
                          properties:
                            key:
                              type: string
                            value:
                              type: string
                          required:
                          - key
                          - value
                          type: object
                        type: array
                    type: object
                required:
                - hardwareType
                - nodeType
                type: object
              port:
                type: string
              user:
                type: string
              vpnConfig:
                properties:
                  ip:
                    type: string
                  vpnServerPublicIP:
                    type: string
                  vpnServerSSHPort:
                    type: string
                  vpnServerSSHUsername:
                    type: string
                  vpnSshCredentialsRef:
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                      namespace:
                        type: string
                    type: object
                type: object
            required:
            - auth
            - host
            - port
            - user
            type: object
          status:
            description: status defines the observed state of RemoteCluster
            properties:
              cnlabSyncRetryCount:
                description: |-
                  CnlabSyncRetryCount is the number of consecutive failures pushing
                  cnlab-runtime credentials to the remote cluster.  When it reaches
                  maxCnlabSyncRetries the controller stops retrying and surfaces an error
                  condition; the VPN link to the remote cluster may be down.
                type: integer
              conditions:
                description: |-
                  conditions is an ordered list of provisioning progress entries.
                  Each entry records one step (success or failure) as it happens, building
                  a full audit trail rather than overwriting prior state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              joinCommand:
                type: string
              message:
                type: string
              phase:
                type: string
              provisionRetryCount:
                description: |-
                  ProvisionRetryCount is the number of consecutive provisioning failures.
                  When it reaches maxProvisionRetries the controller stops retrying and
                  leaves the resource in Failed phase; manual intervention is required.
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
