---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: nodeprovisions.ml.dcn.ssu.ac.kr
spec:
  group: ml.dcn.ssu.ac.kr
  names:
    kind: NodeProvision
    listKind: NodeProvisionList
    plural: nodeprovisions
    singular: nodeprovision
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeProvision is the Schema for the nodeprovisions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NodeProvision
            properties:
              awsConfig:
                description: AWSConfig holds provider-specific parameters for AWS
                  EC2 provisioning.
                properties:
                  ami:
                    description: AMI ID to use for the instance.
                    type: string
                  iamInstanceProfile:
                    description: IAM instance profile name or ARN for the instance.
                    type: string
                  keyPairName:
                    description: EC2 key pair name for SSH access (optional when using
                      cloud-init only).
                    type: string
                  rootVolumeSizeGB:
                    description: |-
                      RootVolumeSizeGB overrides the root EBS volume size in GB.
                      Defaults to 50 GB when unset. The Ubuntu 22.04 AMI default (8 GB) is
                      too small for a Kubernetes node running CRI-O and container images.
                    format: int32
                    type: integer
                  securityGroupIds:
                    description: Security group IDs to attach to the instance.
                    items:
                      type: string
                    type: array
                  subnetId:
                    description: Subnet ID for the instance's primary network interface.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Additional tags to apply to created AWS resources.
                    type: object
                  vpcId:
                    description: VPC ID where the instance will be launched.
                    type: string
                type: object
              credentialsRef:
                description: |-
                  NodeProvisionSpec defines the desired state of NodeProvision
                  CredentialsRef references the Secret containing provider credentials.
                properties:
                  key:
                    description: |-
                      Key is the data key within the secret that holds the credential.
                      When omitted the controller tries well-known names: privateKey, id_rsa,
                      ssh-privatekey, password, key.
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                type: object
              hostname:
                type: string
              instanceId:
                type: string
              instanceType:
                type: string
              ipAddress:
                type: string
              nodeLabel:
                type: string
              provider:
                type: string
              region:
                type: string
              role:
                type: string
              sshPort:
                type: integer
              sshUsernameOverride:
                type: string
            type: object
          status:
            description: status defines the observed state of NodeProvision
            properties:
              completionTime:
                description: Timestamp when provisioning completed.
                format: date-time
                type: string
              hostname:
                description: Assigned hostname.
                type: string
              instanceId:
                description: Provider-generated instance ID (e.g. i-xxxxxxxxxxxxxxxxx
                  for AWS).
                type: string
              ipAddress:
                description: IPAddress is the VPN (WireGuard) IP assigned to the node.
                type: string
              lastUpdated:
                description: LastUpdated is the timestamp of the most recent status
                  update.
                format: date-time
                type: string
              message:
                description: Human-readable status message.
                type: string
              nodeName:
                description: Kubernetes node name after join.
                type: string
              phase:
                description: Current lifecycle phase.
                type: string
              privateIp:
                description: PrivateIP is the cloud-provider private IP of the instance.
                type: string
              progress:
                description: Progress is a 0–100 percentage of provisioning completion.
                type: integer
              publicIp:
                description: PublicIP is the cloud-provider public IP of the instance.
                type: string
              startTime:
                description: Timestamp when provisioning started.
                format: date-time
                type: string
              vpnIp:
                description: VpnIP is the WireGuard VPN IP allocated for this node.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
